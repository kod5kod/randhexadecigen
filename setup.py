#!/usr/bin/env python

from setuptools import setup, find_packages
version = 'v0.1 Beta'
setup(
	name = 'hexadecigen',
	version = version,
	description = 'generates a string or list of hexadecimal strings',
	author = 'kod5kod Datuman',
	url = 'https://github.com/kod5kod',
	include_package_data = True,
	packages = find_packages(),
	install_requires = [
		]
)





